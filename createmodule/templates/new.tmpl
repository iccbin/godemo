<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{print "{{.title}}"}}</title>
</head>
<body>
<form action="/{{pluralize .Name}}/" method="post">
    {{$key,$value := range .FieldMap}}
    <input name="{{lower $key}}" type="text">
    {{end}}
    <input type="submit">
</form>
</body>
</html>